// export const next_endpoint = "http://localhost";
// export const next_port = 3000

import next from "next";

export const endpoints = {
    next_endpoint: "http://localhost",
    next_port: 3000,
    next_fullUrl: "http://localhost:3000",
}